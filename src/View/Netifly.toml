# Créer/Modifier netlify.toml
@"
[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "22.18.0"
"@ | Set-Content netlify.toml